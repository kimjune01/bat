# Install attestation — kimjune01/bat

This file is the receipt of the immune install on this fork. Two PRs were
generated by `/immunize`'s code-gen step against `kimjune01/immune@v0.1`.
Their verdicts are the install attestation: STRONG should land
`immune:trusted`, WEAK should land `immune:suspect`. If either misses its
prediction, immune is miscalibrated and the install needs iteration.

## Expected verdicts

| PR | Branch | Generator | Shape | Expected verdict |
|---|---|---|---|---|
| [#1](https://github.com/kimjune01/bat/pull/1) | `immune/codegen-strong` | opus + manual receipts | real bug-fix in `src/preprocessor.rs` (column-aware tab expansion); HG with H0–H3, attestation file with verified sha256, failing master test → 11/11 passing on branch | `immune:trusted` |
| [#2](https://github.com/kimjune01/bat/pull/2) | `immune/codegen-weak`   | haiku (organic weakness) | small refactor in `src/style.rs::StyleComponentList::from_str()`; no HG, no attestation, body describes WHAT only | `immune:suspect` |

The model split is intentional: STRONG used opus to produce real receipts;
WEAK used haiku precisely because haiku-shaped output naturally lacks the
rigor that triggers receipts. Organic weakness, not instruction-shaped.

## Auto-verification

```bash
bash .immune/verify.sh
```

Exit 0 iff both PRs carry the expected label. Exit 1 with a diff otherwise.

## What "fails" means

| STRONG | WEAK | Diagnosis |
|---|---|---|
| trusted | suspect | calibrated. install attested. |
| suspect or reject | * | filter/attend too strict on receipts; tighten the strong agent brief or loosen attend's threshold |
| trusted | trusted | filter/attend too lenient; tighten attend's receipt presence check |
| reject | reject | the workflow itself isn't running; check Actions tab |
